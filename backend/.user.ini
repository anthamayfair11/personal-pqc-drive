; PHP-FPM 環境用の per-directory 設定
; .htaccess の php_value は mod_php でしか効かないため、PHP-FPM 環境では
; .user.ini で別途指定する必要がある (mixhost は PHP-FPM が多い)

upload_max_filesize = 100M
post_max_size = 110M
memory_limit = 256M
max_execution_time = 60
